import { TransformerContext } from '~/types/handler';

export const getHelpFileTranslation = (
  context: TransformerContext,
  helpFile: string,
): string => {
  // TODO: Look up the help file contents from TransformerContext
  return 'MISSING_HELP_FILE_TRANSLATION';
};
