{
  "name": "flux-monorepo",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/ui",
    "packages/tools"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces --if-present",
    "postinstall": "./scripts/postinst core ui tools"
  },
  "overrides": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^6.3.6",
    "@vitejs/plugin-react": "^4.3.4",
    "@vitejs/plugin-vue": "^5.0.0",
    "vitest": "^3.2.0",
    "typescript": "^5.6.0"
  }
}
